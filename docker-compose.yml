version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "5173:5173" # Maps localhost:5173 to Vite's default port 5173
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped